import { LoaderPlugin } from '../../lib/index.ts';
export declare const useLoaderPlugin: () => {
    plugin: LoaderPlugin | null;
    isLoading: boolean;
    ready: Promise<void>;
};
export declare const useLoaderCapability: () => {
    provides: Readonly<import('../../lib/index.ts').LoaderCapability> | null;
    isLoading: boolean;
    ready: Promise<void>;
};
